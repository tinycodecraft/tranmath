import { RadioProps } from '@mui/material'
import React, { PropsWithChildren } from 'react'
import internal from 'stream'

export as namespace componentSps

interface MenuItemProps {
  href: string
  label: string
}

interface ImageItemProps {
  href: string;
  title: string;
}